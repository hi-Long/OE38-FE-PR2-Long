import { Box, Button } from "@material-ui/core"

const AccountManagementFooter = props => {
    return <Button variant="outlined" fullWidth size="large">
        <Box py={1}>ĐĂNG XUẤT</Box>
    </Button>
}

export default AccountManagementFooter
